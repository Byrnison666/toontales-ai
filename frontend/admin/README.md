# ToonTales Admin

SPA-админка для локального FastAPI backend.

```bash
bun install
bun run dev
```

Приложение откроется на <http://localhost:5173>. Vite проксирует запросы `/api` на `http://localhost:8000`.

Production-сборка:

```bash
bun run build
```
